# Linkwarden send

A simple extension to send a link or a set of links to linkwarden from a context menu

## Usage

Once installed, open the popup ton configure the server and API key.
Then, configure the actions you would like to be able to perform with right click.
Once this is done, save your config and it should now work.
